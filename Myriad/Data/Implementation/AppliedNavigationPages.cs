namespace Myriad.Data
{
    internal class AppliedNavigationPages
    {
        private const string selector = "select text from navigationparagraphs where name=@key order by paragraphindex";
    }
}